name: Create Iris Build (Internal)
on:
  push:
    tags:
      - "*.*.*"
  workflow_dispatch:

env:
  TUIST_TOKEN: ${{ secrets.TUIST_TOKEN }}

jobs:
  validate:
    name: Validate Version Match
    runs-on: macos-26

    # Make sure that the version we are dispatching for is properly set
    steps:
      - uses: actions/checkout@v7
      - name: Validate Version Match
        run: grep "${{ github.ref_name }}" Tuist/ProjectDescriptionHelpers/Settings.swift

  # prerelease:
  #   needs: validate
  #   name: Build Iris
  #   runs-on: macos-26

  #   steps:
  #     - uses: actions/checkout@v7

  #     - name: Install mise & Tools
  #       uses: jdx/mise-action@v4
  #       with:
  #         cache: true

  #     - name: Configure Git for Private Packages
  #       run: |
  #         git config --global url."https://x-access-token:${{ secrets.ACTIONS_PAT }}@github.com/".insteadOf "https://github.com/"

  #     - name: Cache SPM binary artifacts
  #       uses: actions/cache@v4
  #       with:
  #         path: ~/Library/Caches/org.swift.swiftpm/artifacts
  #         key: spm-artifacts-${{ runner.os }}-${{ hashFiles('Tuist/Package.resolved') }}
  #         restore-keys: |
  #           spm-artifacts-${{ runner.os }}-

  #     - name: Pre-fetch SPM binary artifacts
  #       run: |
  #           chmod +x scripts/pre_fetch_spm.sh
  #           ./scripts/pre_fetch_spm.sh

  #     - name: Install Project Dependencies
  #       run: tuist install

  #     - name: Generate Xcode Workspace
  #       run: tuist generate

  #     - name: Run Build and Tests
  #       run: tuist test --skip-ui-tests

  # notarize:
  #   needs: build
  #   name: Notarize Iris
  #   runs-on: macos-26

  #   steps:
  #   - name: Make Executable Scripts
  #     run: |
  #       chmod +x scripts/release.sh scripts/sentry.sh  scripts/update_sparkle.sh
  #   - name: Make tmp
  #     run: |
  #       rm -rf tmp
  #       mkdir tmp
  #       mkdir tmp/compressed
  #       mkdir tmp/archive
  #       mkdir tmp/apps
  #   - name: Release App
  #     run: ./scripts/release.sh
  #     env:
  #       APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
  #       APPLE_API_ISSUER_ID: ${{ secrets.APPLE_API_ISSUER_ID }}
  #       APPLE_API_KEY_BASE64: ${{ secrets.APPLE_API_KEY_BASE64 }}

  #   - name: Upload Sentry Information
  #     run: ./scripts/sentry.sh ${{ secrets.IRIS_SENTRY_AUTH_TOKEN }}

  # create_artifacts:
  #   needs: notarize
  #   name: Create Artifacts (tar, dmg)
  #   runs-on: macos-26

  #   steps:
  #   - name: Create Artifacts
  #     run: ./scripts/create_artifacts.sh

  # upload_artifacts:
  #   if: startsWith(github.ref, 'refs/tags/')
  #   needs: create_artifacts
  #   name: Upload Artifacts to GitHub
  #   runs-on: macos-26

  #   steps:
  #   - name: Release
  #     uses: softprops/action-gh-release@v3
  #     with:
  #       prerelease: true
  #       generate_release_notes: true
  #       files: |
  #         tmp/apps/iris.tar.xz
  #         tmp/apps/iris.dmg
